server:
  port: 10265

jwt:
  secret: dev-secret
  cookie-name: jwt

eureka:
  client:
    service-url:
      defaultZone: ${DEV_EUREKA_URL}
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true
